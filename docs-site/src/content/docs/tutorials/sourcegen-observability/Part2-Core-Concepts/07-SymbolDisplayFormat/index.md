---
title: "SymbolDisplayFormat"
---

다양한 심볼 표시 형식을 비교합니다.

## 핵심 개념

- `FullyQualifiedFormat` — `global::` 접두사 포함
- `MinimallyQualifiedFormat` — 최소 한정 이름
- 커스텀 `SymbolDisplayFormat` 생성

## 실행

```bash
dotnet run --project SymbolDisplayFormat
```
