---
title: "데이터 모델"
---

소스 생성기에서 사용하는 데이터 모델 패턴을 보여줍니다.

## 핵심 개념

- `readonly record struct` — 불변 값 타입, 값 동등성 자동 지원
- `class` 기반 모델 — 복잡한 계층 데이터용

## 실행

```bash
dotnet run --project DataModels
```
