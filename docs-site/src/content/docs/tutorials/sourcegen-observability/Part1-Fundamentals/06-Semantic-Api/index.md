---
title: "06: Semantic API"
---

Roslyn Semantic API로 심볼 정보를 분석합니다.

## 핵심 개념

- `GetDeclaredSymbol()` — 선언된 심볼 조회
- `GetSymbolInfo()` — 표현식의 심볼 정보
- `GetTypeInfo()` — 타입 정보 조회

## 실행

```bash
dotnet run --project SemanticApi
```
