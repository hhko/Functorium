---
title: Functorium
description: 도메인 주도 설계(DDD)와 함수형 아키텍처 원칙 기반의 .NET 아키텍처 프레임워크
template: splash
hero:
  tagline: |
    코드가 비즈니스를 말하고, 운영이 코드를 읽는다.
    Functional + DDD + Observability 통합 .NET 프레임워크
  image:
    html: '<img src="/Functorium/Functorium.png" alt="Functorium logo" width="400" height="400" />'
  actions:
    - text: 5분 빠른시작
      link: /Functorium/quickstart/
      icon: right-arrow
    - text: GitHub
      link: https://github.com/hhko/Functorium
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard, Steps, Aside } from '@astrojs/starlight/components';

## 누구를 위한 프레임워크인가

- **엔터프라이즈 DDD를 실천하는 .NET 팀** — 도메인 모델의 불변성과 합성 가능성을 코드 수준에서 보장하고 싶은 팀
- **개발과 운영 사이의 언어 격차를 해소하고 싶은 팀** — 도메인 개념이 코드, 로그, 메트릭, 추적 정보에 일관되게 반영되어야 하는 팀
- **함수형 DDD 아키텍처를 체계적으로 도입하려는 아키텍트** — LanguageExt + DDD + OpenTelemetry를 통합된 프레임워크로 사용하고 싶은 설계자

---

Mediator, LanguageExt, FluentValidation, OpenTelemetry는 각각 훌륭합니다.
하지만 이들을 일관된 DDD 아키텍처로 통합하려면 에러 전파, 파이프라인 순서, 관측성 경계, 타입 제약에 대한 수백 가지 결정이 필요합니다.
**Functorium은 이 결정을 한 번, 일관되게 내립니다.**

### 풀고자 하는 문제

<div class="problem-card">
  <span class="text-problem">예외가 곧 비즈니스 로직</span> — 성공과 실패가 try-catch로 분기되어 흐름을 예측하기 어렵고, 도메인 규칙의 합성이 불가능합니다.
</div>
<div class="problem-card">
  <span class="text-problem">개발 언어 ≠ 운영 언어</span> — 기능 명세와 운영 요구사항이 서로 다른 체계로 관리되어 해석 차이가 누적됩니다.
</div>
<div class="problem-card">
  <span class="text-problem">Observability는 항상 사후 보완</span> — 로그, 메트릭, 트레이싱이 구현 완료 후 부착되어 장애 시 맥락이 유실됩니다.
</div>

### 돌파 방향

<div class="solution-card">
  <span class="text-solution">예외 없는 순수 도메인</span> — Fin&lt;T&gt;, FinT&lt;IO, T&gt;로 결과와 사이드 이펙트를 타입 수준에서 명시하고, LINQ 합성으로 도메인 흐름을 구성합니다.
</div>
<div class="solution-card">
  <span class="text-solution">단일 도메인 언어로 통합</span> — Bounded Context를 명확히 정의하여 도메인 개념이 코드, 문서, 운영 메트릭에 일관되게 반영됩니다.
</div>
<div class="solution-card">
  <span class="text-solution">설계에 내재된 Observability</span> — OpenTelemetry 기반 Logging, Metrics, Tracing이 유스케이스 파이프라인에 자동 적용됩니다.
</div>

---

## 시작하기

<Steps>

1. **Naive 코드 — 우리가 흔히 작성하는 코드**

   온라인 상점에서 상품을 관리한다고 가정합니다. 가장 직관적인 코드는 이렇습니다:

   ```csharp
   public class Product
   {
       public string Name { get; set; } = "";     // 빈 문자열? null?
       public decimal Price { get; set; }         // 음수? 0?
   }
   ```

   이 코드는 컴파일러가 아무 문제도 발견하지 못합니다. 하지만 실행하면:

   - `Name = ""` — 빈 상품명이 DB에 저장됩니다. 카탈로그에 이름 없는 상품이 노출됩니다.
   - `Price = -500` — 음수 가격이 결제 시스템에 전달됩니다. 고객에게 돈을 지급하게 됩니다.
   - `new Product()` — 어디서든 검증 없이 생성할 수 있습니다. 서비스 레이어, 테스트, 심지어 컨트롤러에서도.
   - `Name`과 `Description`이 모두 `string` — 컴파일러가 둘을 구분할 수 없어 실수로 바꿔 넣어도 발견되지 않습니다.

   이 버그들은 코드 리뷰에서 발견되지 않고, 단위 테스트를 통과하고, **프로덕션에서 고객이 발견합니다.**
   DDD의 Value Object는 이 모든 문제를 **컴파일 타임에 차단합니다.**

3. **Value Object — 규칙을 타입으로 강제**

   ```csharp
   public sealed class ProductName : SimpleValueObject<string>
   {
       public const int MaxLength = 100;
       private ProductName(string value) : base(value) { }

       public static Fin<ProductName> Create(string? value) =>
           CreateFromValidation(Validate(value), v => new ProductName(v));

       public static Validation<Error, string> Validate(string? value) =>
           ValidationRules<ProductName>
               .NotNull(value)              // 실패 시 에러 코드 "DomainErrors.ProductName.Null"
               .ThenNotEmpty()              // 실패 시 에러 코드 "DomainErrors.ProductName.Empty"
               .ThenNormalize(v => v.Trim())
               .ThenMaxLength(MaxLength);   // 실패 시 에러 코드 "DomainErrors.ProductName.TooLong";
   }
   ```

   `ProductName.Create("")`은 `Fin.Fail`을 반환합니다. 예외 없이, 에러 코드와 함께.

4. **AggregateRoot — 상태 변경을 이벤트로**

   ```csharp
   [GenerateEntityId]  // → ProductId 자동 생성 (Ulid 기반)
   public sealed class Product : AggregateRoot<ProductId>
   {
       public sealed record CreatedEvent(
           ProductId ProductId, ProductName Name, Money Price) : DomainEvent;

       public ProductName Name { get; private set; }
       public Money Price { get; private set; }

       private Product(ProductId id, ProductName name, Money price) : base(id)
       {
           Name = name;
           Price = price;
       }

       public static Product Create(ProductName name, Money price)
       {
           var product = new Product(ProductId.New(), name, price);
           product.AddDomainEvent(new CreatedEvent(product.Id, name, price));
           return product;
       }
   }
   ```

   `new Product()` 불가. 검증된 VO로만 생성. 생성 시 `CreatedEvent` 자동 발행.

5. **Command Usecase — 예외 없는 비즈니스 흐름**

   ```csharp
   public sealed class CreateProductCommand
   {
       public sealed record Request(string Name, decimal Price)
           : ICommandRequest<Response>;
       public sealed record Response(string ProductId, string Name, decimal Price);

       public sealed class Usecase(IProductRepository repository)
           : ICommandUsecase<Request, Response>
       {
           public async ValueTask<FinResponse<Response>> Handle(
               Request request, CancellationToken ct)
           {
               var name = ProductName.Create(request.Name).Unwrap();
               var price = Money.Create(request.Price).Unwrap();
               var product = Product.Create(name, price);

               FinT<IO, Response> usecase =
                   from created in repository.Create(product)
                   select new Response(
                       created.Id.ToString(), created.Name, created.Price);

               return (await usecase.Run().RunAsync()).ToFinResponse();
           }
       }
   }
   ```

   `from ... in ... select` LINQ 합성으로 예외 없이 비즈니스 흐름을 조립합니다.

</Steps>

<Aside type="tip" title="5분 빠른시작에서 이 코드를 직접 실행해 보세요">
[Quickstart](/Functorium/quickstart/)에서 프로젝트 생성부터 테스트까지 5분 안에 완료할 수 있습니다.
</Aside>

---

## 무엇이 달라지는가

<div class="compare-grid">
  <div class="compare-before">
    <h4>Naive 코드</h4>
    <ul>
      <li><code>string Name</code> — 아무 값이나 허용</li>
      <li><code>new Product()</code> — 어디서든 생성</li>
      <li><code>try-catch</code>로 에러 처리</li>
      <li>예외 발생 시 스택 트레이스만</li>
      <li>로그/메트릭 수동 추가</li>
      <li>이벤트 없이 상태 변경</li>
    </ul>
  </div>
  <div class="compare-after">
    <h4>Functorium</h4>
    <ul>
      <li><code>ProductName</code> — 빈 문자열, 100자 초과 방지</li>
      <li><code>Product.Create(vo1, vo2)</code> — 검증된 VO로만 생성</li>
      <li><code>Fin&lt;T&gt;</code>로 성공/실패를 타입으로 명시</li>
      <li><code>"DomainErrors.ProductName.Empty"</code> 구조화된 에러 코드</li>
      <li><code>[GenerateObservablePort]</code>로 자동 관측성</li>
      <li><code>AddDomainEvent(new CreatedEvent(...))</code> 자동 발행</li>
    </ul>
  </div>
</div>

---

## 핵심 기능

<CardGrid>
  <Card title="타입 안전한 도메인 모델링" icon="pencil">
    Value Object, Entity, AggregateRoot, Specification으로 불변식을 코드에 내장합니다.
    `Fin<T>`와 `FinT<IO, T>`로 예외 없이 도메인 흐름을 합성합니다.
  </Card>
  <Card title="자동 코드 생성" icon="setting">
    Source Generator가 Observable wrapper, EntityId, CtxEnricher를 컴파일 시점에 생성합니다.
    반복 코드를 작성할 필요가 없습니다.
  </Card>
  <Card title="CQRS 파이프라인" icon="approve-check">
    Command/Query를 분리하고, 8단계 파이프라인이 유효성 검사, 트랜잭션, Observability를 자동 적용합니다.
  </Card>
  <Card title="Observability by Design" icon="open-book">
    OpenTelemetry 기반 Logging, Metrics, Tracing이 설계 단계부터 내재화됩니다.
    `ctx.*` 3-Pillar Enrichment로 비즈니스 컨텍스트를 자동 전파합니다.
  </Card>
</CardGrid>

