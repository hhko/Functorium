# 05: INamedTypeSymbol

INamedTypeSymbol의 주요 속성을 탐색합니다.

## 핵심 개념

- `Interfaces`, `BaseType` — 타입 계층 탐색
- `ContainingNamespace` — 네임스페이스 정보
- `Constructors` — 생성자 정보
- `TypeParameters` — 제네릭 타입 파라미터

## 실행

```bash
dotnet run --project NamedTypeSymbol
```
