# 05: Custom Generator Template

커스텀 생성기 템플릿 패턴(AutoMapper 예제)을 보여줍니다.

## 실행

```bash
dotnet run --project CustomGeneratorTemplate.Usage
dotnet test --project CustomGeneratorTemplate.Tests.Unit
```
