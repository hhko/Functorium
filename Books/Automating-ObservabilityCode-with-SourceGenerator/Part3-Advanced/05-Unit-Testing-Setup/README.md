# 05: Unit Testing Setup

CSharpGeneratorDriver를 사용한 소스 생성기 단위 테스트 설정을 보여줍니다.

## 핵심 개념

- `CSharpGeneratorDriver` — 생성기 실행 환경
- `GeneratorDriverRunResult` — 결과 검증

## 실행

```bash
dotnet run --project UnitTestingSetup.Usage
```
