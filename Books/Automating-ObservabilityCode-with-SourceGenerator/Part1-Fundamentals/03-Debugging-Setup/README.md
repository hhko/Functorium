# 03: Debugging Setup

소스 생성기 디버깅 설정 방법을 보여줍니다.

## 핵심 개념

- `Debugger.Launch()` — 생성기 실행 시 디버거 연결
- 조건부 디버깅 — `#if DEBUG` 활용

## 실행

```bash
dotnet run --project DebuggingSetup.Usage
```
