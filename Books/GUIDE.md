# Books 폴더 문서 작성 가이드

이 폴더의 마크다운 문서를 작성할 때 따라야 할 지침입니다.

---

## 중첩 코드 블록

마크다운 예시를 보여줄 때 코드 블록 안에 또 다른 코드 블록이 포함되면 GitHub에서 렌더링이 깨집니다.

### 문제 상황

```markdown
## 예시
```csharp
var x = 1;
```
```

위와 같이 작성하면 내부 코드 블록이 외부 블록을 닫아버려 렌더링이 깨집니다.

### 해결 방법

외부 코드 블록에 백틱 4개(``````)를 사용합니다:

`````markdown
````markdown
## 예시
```csharp
var x = 1;
```
````
`````

### 주의: 2단계 중첩도 동일

마크다운 템플릿 예시처럼 **2단계 중첩**도 같은 문제가 발생합니다:

**잘못된 예 (렌더링 깨짐):**

```
```markdown
**형식:**
```markdown
---
title: 제목
---
```
```
```

내부의 ` ```markdown `이 외부 블록을 닫아버립니다.

**올바른 예:**

``````
````markdown
**형식:**
```markdown
---
title: 제목
---
```
````
``````

**규칙**: 내부에 백틱 3개 코드 블록이 있으면 외부는 반드시 백틱 4개 사용

---

## ASCII 박스 정렬

한글과 영문이 섞인 ASCII 박스를 그릴 때 고정폭 폰트에서 정렬이 맞지 않을 수 있습니다.

### 원인

- 한글 문자: 2칸 차지
- 영문/숫자/기호: 1칸 차지

### 해결 방법

한글 문자 수만큼 공백을 줄여서 보정합니다:

```
영문만:    │ Hello World │  (11자 + 공백 2개 = 13칸)
한글 포함: │ 안녕 World  │  (안녕=4칸 + World=5칸 + 공백 2개 = 11칸 → 13칸)
```

---

## 코드 블록 언어 지정

모든 코드 블록에 언어를 명시합니다:

| 언어 | 지정자 |
|------|--------|
| C# | `csharp` |
| Bash/Shell | `bash` |
| YAML | `yaml` |
| JSON | `json` |
| Markdown | `markdown` |
| 일반 텍스트 | `txt` |
| Diff | `diff` |

---

## 체크리스트

문서 작성 완료 전 확인:

- [ ] 중첩 코드 블록이 있으면 외부에 백틱 4개 사용
- [ ] ASCII 박스의 한글 정렬 확인
- [ ] 모든 코드 블록에 언어 지정
- [ ] GitHub에서 렌더링 미리보기 확인
