# 도메인 용어 사전 (Domain Glossary)

<!-- 이 템플릿은 새 호스트 프로젝트의 도메인 계층에 DOMAIN-GLOSSARY.md로 복사하여 사용한다. -->
<!-- 각 섹션의 {placeholder}를 실제 도메인에 맞게 채운 뒤 이 주석들을 제거한다. -->

## 1. 도메인 개요

<!-- 도메인의 핵심 영역과 목적을 1~2문장으로 기술한다. -->
<!-- Bounded Context 경계가 있다면 명시한다. 단일 Context이면 그 사실을, 복수이면 각 Context의 범위를 기술한다. -->

이 도메인은 **{영역1}**, **{영역2}** 등 {N}개 핵심 영역으로 구성된 {시스템 설명}이다.

## 2. 도메인 용어 사전 (Ubiquitous Language)

<!-- 유형: 애그리거트 루트, 엔티티, 값 객체, 공유 엔티티, 공유 값 객체 -->

| 용어 | 영문 | 유형 | 정의 |
|------|------|------|------|
| {용어} | {EnglishName} | {유형} | {정의} |

## 3. 애그리거트별 상세

### {애그리거트명}({EnglishName})

- **속성**: {속성 목록}
- **기능**: {기능 목록}
- **불변 조건**:
  - {조건1}
  - {조건2}
- **조회 규칙**: {Specification 기반 조회 규칙}

<!-- 아래는 해당하는 경우에만 추가하는 선택 항목이다. -->
<!-- - **자식 엔티티 — {엔티티명}({EnglishName})**: Aggregate 경계 내 Child Entity가 있을 때. 속성/불변 조건을 기술한다. -->
<!-- - **교차 규칙**: 다른 애그리거트와의 참조 방식(ID 참조 등)을 명시할 때. -->
<!-- - **동시성**: 낙관적 잠금(RowVersion) 등 동시성 제어가 있을 때. -->
<!-- - **생명주기 관리**: 독립 Repository로 관리되거나 특수한 생명주기가 있을 때. -->
<!-- - **설계 결정 근거**: 애그리거트 경계, 승격/분리 등 설계 결정의 이유를 기록할 때. -->

<!-- 애그리거트마다 위 블록을 반복한다. -->

## 4. 애그리거트 간 관계

<!-- ASCII 다이어그램으로 애그리거트 간 참조/의존 관계를 표현한다. -->
<!-- 포함 관계(Child Entity)는 ──(포함)──▶ 으로, ID 참조는 ──(ID 참조)──▶ 으로 구분한다. -->

```
범례: 이름 = 애그리거트 루트 | [이름] = 자식 엔티티 | *이름* = 공유 애그리거트 루트 | [*이름*] = 공유 자식 엔티티
범례: ──▶ = ID 참조/포함 | ┄┄▷ = 도메인 서비스 검증

{AggregateA} ──({관계 설명})──▶ {AggregateB}
{AggregateC} ──(포함)──▶ [{ChildEntity}]
*{SharedAggregate}* ◀──({ID 참조})── {AggregateD}
{AggregateA} ┄┄({서비스명})┄┄▷ {AggregateB}
```

- **{A} → {B}**: {관계 설명}

## 5. 도메인 서비스 — 교차 애그리거트 비즈니스 규칙

<!-- 단일 애그리거트로 표현할 수 없는 비즈니스 규칙이 있을 때만 작성한다. -->
<!-- 없으면 이 섹션을 "해당 없음"으로 표기하거나 제거한다. -->

### {서비스명} ({EnglishName})

- **목적**: {목적}
- **규칙**: {규칙 설명}
- **반환 타입**: {반환 타입}

<!-- 도메인 서비스가 있을 때, Invariant 검증 위치 설계 결정을 표로 정리한다. -->
<!-- 단일 Aggregate 규칙 vs 교차 Aggregate 규칙의 검증 위치 근거를 명시한다. -->

### Invariant 검증 위치 설계 결정

| 검증 유형 | 위치 | 근거 |
|-----------|------|------|
| {단일 Aggregate 규칙} | {Aggregate 내부} | {근거} |
| {교차 Aggregate 규칙} | {도메인 서비스} | {근거} |

## 6. 도메인 이벤트 (상태 변경 시 발행)

<!-- 도메인 이벤트가 없으면 이 섹션을 "해당 없음"으로 표기하거나 제거한다. -->

| 이벤트 | 발생 시점 | 포함 정보 |
|--------|-----------|-----------|
| {Aggregate}.{Event} | {시점} | {정보} |

## 7. 핵심 제약조건(불변 조건) 요약

<!-- 섹션 3의 불변 조건을 한눈에 볼 수 있도록 표로 정리한다. -->

| 분류 | 제약조건 | 오류 |
|------|----------|------|
| {분류} | {제약조건} | {오류 또는 처리 방식} |

---

> 구현 패턴(값 객체, 애그리거트, 리포지토리, Specification, 함수형 타입, 도메인 이벤트 등)은
> [DOMAIN-IMPLEMENTATION-PATTERNS.md](DOMAIN-IMPLEMENTATION-PATTERNS.md)를 참조한다.
